import SwiftUI

struct AppTitleView: View {
    var body: some View {
        Text("Menu Bar Thermostat")
            .font(.title)
            .fontWeight(.medium)
            .foregroundColor(.primary)
    }
}
